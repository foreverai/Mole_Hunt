###:kivy 1.7.2
###Give widgets ids to allow them 
###to be manipulated on events 
###and in kv file and in the py file
	
###Creates label with common properties, so everything doesn't need retyped	
<DisplayScore@Label>        
	font_size: 30

<DisplayText@Label>
	font_size: 40

<Controller>:
    canvas.before:
        Color:
            rgba: 0, 0, 0, 1
        Rectangle:
            size: self.size
            pos: self.pos
       
	BoxLayout:
		orientation: 'vertical'
		###Sizes the layout to the size of the root widget (the size of the screen)
		size: root.size					
		
		###Contains the display labels
		BoxLayout:							
			orientation: 'horizontal'
			size_hint: 1, 0.2
			
			GridLayout: 
				cols: 3
				
				DisplayText:
					text: 'Score'
					
				DisplayText:
					text: 'TEST'
					
				DisplayText:
					text: 'Time'		
				          	
				DisplayScore:
					text: str(root.score_label)
					
				DisplayScore:
					text: str(root.high_score_label)
					
				DisplayScore: 
					text: str(root.time_label)
		
		###Displays the game box
		FloatLayout:				
			id: layout
			size_hint: 1, 0.85
			canvas.before:
		        Color:
		            rgba: 0.702, 0.957, 0.988, 1
		            
		        Rectangle:
		            pos: self.pos
		            size: self.size
		    
		    ###Need this line to put the ball on the main game screen                
		    Ball:		
		    	        
			Button:
				id: Begin
				background_normal: 'Images/play.png'
				size_hint: 0.18, 0.18
				text: "PLAY"
				center: self.parent.center
				on_press: root.restart_game()
				on_press: self.parent.remove_widget(Begin)
				
				
<Ball>
	id: ball
	size: 30, 30
	Button:
		background_normal: 'Images/mole.png'
		background_color: 3, 1, 1, 1
		center: root.ball_center
		on_press: root.move()				
	